# Runtime (tutte già presenti in <local-user>/anaconda3, Python 3.12)
numpy>=1.26
scipy>=1.13
pandas>=2.0
streamlit>=1.45      # UI didattica (porta anche tornado)
tornado>=6.4         # server Lab PRO (labpro/)
plotly>=5.24

# Sviluppo/test
pytest>=7.4

# Opzionali (pagina canale / riferimenti)
scikit-rf>=2.0
serdespy>=1.0
